name = "ganeshagame-new"
compatibility_date = "2026-09-20"
pages_build_output_dir = "."

# Cloudflare Pages Functions live in /functions at repository root.
# Bind the D1 database to variable name DB in Cloudflare Pages settings.
