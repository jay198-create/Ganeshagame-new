# September 20 repair

The Journey memory game is restored to the original position-recall rules: briefly reveal Ganeshas, hide them, then select their locations. Easy, Medium and Hard each retain a 100-level map. Targets and board size rise gradually while preview time falls. Three mistakes require a retry. Pause hides the board and preserves remaining preview time.

Cloud requests time out after four seconds instead of blocking startup indefinitely. Missing scripts show a retry screen. Returning from festival screens no longer waits for cloud sync. Cloud recovery still requires a configured D1 binding and recovery key.

105 idol choices now reference local artwork: 45 individual images and 60 figures from three existing generated catalog sheets. Artwork is packed for faster delivery. Catalog-derived figures are lower resolution than individual images. Original attachment filenames are retained in idol-art.js. No artwork generation was needed.

Final-day puja must be completed before visarjan. Duplicate item purchases cannot charge Modaks twice.

Cloudflare Pages: retain the existing GitHub main branch, repository-root output directory `.`, /functions handlers, and DB binding. This update does not alter your database or deployment settings.

Automated DOM and rule tests cover restored memory, progression and a nonresponding save service. Real-device rendering and live Cloudflare deployment are not verified here.

Math now generates fresh expressions on every attempt, avoids the last 20 expressions, and scales number ranges and operation types by difficulty and level. Hard adds multi-step expressions and percentages. My Festival uses a rendered-art banner, larger idol portraits, gallery lighting and restrained colors. These are 2D artworks, not real-time 3D.
